"hello everyone" 
"i am coming home soon" 
"'man' command is used to see manual for a particular command. It is equivalent of '--help' in windows OS" 
"to jump between words use 'ALT + F/B'" 
"absolute or full path points to the same location in a file system, regardless of the current working directory. To do that, it must include the root directory. By contrast, a relative path starts from some given working directory, avoiding the need to provide the full absolute path." 
"Shell is a program which processes commands and returns output , like bash in Linux . Terminal is a program that run a shell , in the past it was a physical device (Before terminals were monitors with keyboards, they were teletypes) and then its concept was transferred into software" 
"a flag is an extra value added to be more specific on a command" 
"examples of flag: ls -a, cp -r, ls -la" 
"when you flaf rf in rm you remove the entire contents in the folder" 
